rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /images/{allPaths=**} {
      allow read: if true;
      // Replace with YOUR Google account email after setup:
      allow write: if request.auth != null
        && request.auth.token.email == 'YOUR_EMAIL@gmail.com';
    }
  }
}
